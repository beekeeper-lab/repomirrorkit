# /commands Command

Prints a quick-reference table of all available slash commands with usage and flags.

## Usage

```
/commands
```

No arguments or flags. Outputs a static table — instant, no tool calls.
