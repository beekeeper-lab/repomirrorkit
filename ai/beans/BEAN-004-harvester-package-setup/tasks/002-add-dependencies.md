# Task 002: Add Click Dependency

| Field | Value |
|-------|-------|
| **Owner** | Developer |
| **Status** | Pending |
| **Depends On** | — |

## Objective

Add `click` to `[project.dependencies]` in `pyproject.toml`.

## Acceptance Criteria

- `click` listed in `[project.dependencies]`
- `uv pip install -e ".[dev]"` succeeds
