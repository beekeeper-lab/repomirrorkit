# Task 001: Add structlog dependency

| Field | Value |
|-------|-------|
| **Owner** | Developer |
| **Status** | Pending |
| **Depends On** | — |

## Objective

Add `structlog` to project dependencies in `pyproject.toml`.

## Acceptance Criteria

- [ ] `structlog` added to `[project.dependencies]`
- [ ] `uv pip install -e ".[dev]"` succeeds
