# Sample Next.js App

A minimal Next.js (App Router) fixture for the harvester end-to-end integration test (BEAN-050).
Includes one page, one component, one API route, and one model — enough to exercise the
Next.js detector, route/component/api/model analyzers, and Stage G project-folder generation.
