# Sample Flask App

A minimal Flask + SQLAlchemy fixture for the harvester end-to-end integration test (BEAN-050).
Two routes (GET/POST `/api/users`) and one SQLAlchemy `User` model are enough to exercise
the Python API detector, model analyzer, and route analyzer paths through Stage G.
